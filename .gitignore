*.exe
*.exe~
*.dll
*.so
*.dylib

*.test

*.out
coverage.*
*.coverprofile
profile.cov

go.work.sum

.env
.envrc

# infra/
dist*
.goreleaser-extras/
# Generated by `just start-demo` from terraform output — one line per
# exerciser port keyed to the current AWS IP, so it churns every deploy.
internal/dev/demo/exerciser_targets.json

/tasks/

.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info
# commit .terraform.lock.hcl for teams, but noisy for solo projects
.terraform.lock.hcl
.terraformrc
terraform.rc

to_llm.sh
/pln
.worktrees/

# Build artifacts
pln-linux
pln-local
.pln-*
/exerciser
/exerciser-linux
/exercisor
/sink

# Local justfile (personal deploy recipes)
.justfile.local
